package chapter4;

public class PreKnowledge {
	
	//Binary tree: Binary search tree;
	/*
	 * Binary Tree: parent node has two children, left and right
	 * Binary Search Tree: parent node has two children, and left is less than parent less than right node;
	 * 
	 */

	//Balanced: Unbalanced;
	/*
	 * Not all trees are balanced;
	 * When not balanced: describe average and worst case;
	 * Balanced: means the depth of subtrees vary not more than a certain amount;
	 *  
	 */
	
	//Full and complete trees;
	/*
	 * Extremely rare: all leaves are at the bottom of the tree;
	 * all non-leave nodes have exactly two children;
	 * 
	 */
	
	//Binary Tree Traversal;
	/*
	 * in-order; post-order; pre-order;
	 * 
	 */
	
	//Graph Traversals;
	/*
	 * DFS; problematic if we need to search through the shorter nodes, it may lead to a search of 1000 anscestors.
	 * BFS;
	 * 
	 * DFS: When applied for Graphs, we must check whether a node has been visited. Or we may end up in infinite loop.
	 * 
	 */
	
	
}
